---
description: Python stack is frozen — do not modify
alwaysApply: true
---

# Python stack frozen

Каталог `fatty/`, `main.py`, `requirements.txt`, `FaTTY.spec` и прочий Python/PyInstaller-стек **заморожены**.

Не править, не рефакторить, не «подтягивать» к C++-порту и не удалять, пока Amour явно не попросит. Вся доработка — только C++ (`src/`, CMake, `build.bat`, CI).
